# Python
__pycache__/
*.py[cod]
*.pyd
*.pyo
*.egg-info/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# Package managers
uv.lock
poetry.lock

# Test / build artefacts
.pytest_cache/
.coverage
htmlcov/
dist/
build/

# Large generated files (not committed — too big or re-generatable)
sft_data.jsonl
generations.jsonl
training_log.json

# Model checkpoints (too large for git — download from HF Hub instead)
sft-checkpoint/
data-centric-checkpoints/
data-centric-adapter/
data-centric-merged/

# Training logs — COMMIT THESE after a real run (judges require them)
# logs/training.jsonl   <- per-episode reward JSONL
# logs/grpo/            <- TensorBoard event files
# Un-comment above ONLY if you deliberately want to exclude them.

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Hackathon docs (keep local only)
*.pdf
\[External\]*/
